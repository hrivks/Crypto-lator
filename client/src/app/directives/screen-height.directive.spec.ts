import { ScreenHeightDirective } from './screen-height.directive';

describe('ScreenHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenHeightDirective();
    expect(directive).toBeTruthy();
  });
});
