import { FilterCurrencyPipe } from './filter-currency.pipe';

describe('FilterCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
